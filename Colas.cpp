#include <iostream>
using namespace std;
int main()
{
    queue<int> q;
    q.push(x); // insertar
    q.pop(); // Eliminar 
    q.front(); // primer valor en la cola
    q.back(); // ultimo valor 
    // tambien aplica size y empty
    
    q.size(); 
    q.empty(); 
    

    return 0;
}